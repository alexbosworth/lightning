import {AuthenticatedLightningMethod} from '../../typescript';

/**
 * Delete all records of payments
 *
 * Requires `offchain:write` permission
 */
export const deletePayments: AuthenticatedLightningMethod;
