import {AuthenticatedLightningMethod} from '../../typescript';

/**
 * Delete all forwarding reputations
 *
 * Requires `offchain:write` permission
 */
export const deleteForwardingReputations: AuthenticatedLightningMethod;
